export type VisitorSession = {
  id: string;
  created_at: string;
  visitor_id: string;
  duration: number;
};

export type ViewsChartSession = {
  id: string;
  created_at: string;
  visitor_id: string;
};
